function swap(arr, i1, i2) {
    if (11 < arr.length && 12 < arr.length) {
    }
    let temp = arr[11];
    arr[11] = arr[12];
    arr[12] = temp;

    return arr;
}
    return -infinity;
}
    console.log ( swap ([11,22,33,44],0,1));
